import { useState } from "react";
import { ArrowLeft, ArrowRight, LockKeyhole, Mail, ShieldCheck, Sparkles } from "lucide-react";
import { Link, useLocation } from "wouter";
import AppShell from "@/components/AppShell";
import { Field } from "@/components/PortalComponents";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { api } from "@/lib/api";

export default function Login() {
  const [, navigate] = useLocation();
  const [form, setForm] = useState({ email: "", password: "" });
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [serverError, setServerError] = useState("");
  const [submitting, setSubmitting] = useState(false);
  function update(key: keyof typeof form, value: string) { setForm(prev => ({ ...prev, [key]: value })); setErrors(prev => ({ ...prev, [key]: "" })); setServerError(""); }
  async function submit(event: React.FormEvent) { event.preventDefault(); const next: Record<string, string> = {}; if (!form.email.trim()) next.email = "Email is required."; else if (!/^\S+@\S+\.\S+$/.test(form.email)) next.email = "Enter a valid email address."; if (!form.password) next.password = "Password is required."; setErrors(next); if (Object.keys(next).length) return; try { setSubmitting(true); await api.auth.login(form); navigate("/dashboard"); } catch (error) { setServerError(error instanceof Error ? error.message : "Unable to log in right now."); } finally { setSubmitting(false); } }
  return <AppShell><div className="mx-auto grid max-w-7xl gap-10 px-5 py-12 sm:px-8 sm:py-20 lg:grid-cols-[0.85fr_1.15fr] lg:items-center lg:px-10"><div className="hidden rounded-[30px] bg-primary p-10 text-primary-foreground shadow-[0_20px_55px_rgba(12,107,88,0.16)] lg:block"><div className="flex items-center gap-2 font-mono text-[11px] uppercase tracking-[0.2em] text-[#b9e1c4]"><Sparkles className="size-4" />Secure access</div><h1 className="mt-10 max-w-md text-5xl font-extrabold leading-[1.02] tracking-[-0.07em]">Pick up where your campus story left off.</h1><p className="mt-6 max-w-md text-sm leading-7 text-[#c9e2d0]">Your dashboard is protected by token-based authentication and keeps your student activity private.</p><div className="mt-14 grid gap-4 border-t border-white/15 pt-7 text-sm"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-white/10"><ShieldCheck className="size-4" /></span><span>Password-protected student account</span></div><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-white/10"><LockKeyhole className="size-4" /></span><span>JWT-secured API requests</span></div></div></div><div className="mx-auto w-full max-w-lg"><Link href="/" className="mb-8 inline-flex items-center gap-2 text-sm font-bold text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" />Back to home</Link><Card className="border-0 bg-card shadow-[0_18px_50px_rgba(36,66,53,0.08)]"><CardContent className="p-6 sm:p-9"><div className="mb-8"><div className="mb-4 grid size-11 place-items-center rounded-2xl bg-secondary text-primary"><Mail className="size-5" /></div><h2 className="text-3xl font-extrabold tracking-[-0.055em]">Log in to CampusConnect</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Use your registered university email to access your protected student space.</p></div><form onSubmit={submit} className="space-y-5" noValidate><Field id="login-email" label="University email" type="email" autoComplete="email" placeholder="you@university.edu" value={form.email} onChange={e => update("email", e.target.value)} error={errors.email} /><Field id="login-password" label="Password" type="password" autoComplete="current-password" placeholder="Enter your password" value={form.password} onChange={e => update("password", e.target.value)} error={errors.password} />{serverError && <p className="rounded-xl bg-[#fff0ee] p-3 text-xs font-semibold text-destructive">{serverError}</p>}<Button type="submit" disabled={submitting} className="mt-2 h-12 w-full rounded-xl bg-primary font-extrabold shadow-[0_10px_22px_rgba(12,107,88,0.18)] hover:bg-[#0a5d4d]">{submitting ? "Verifying credentials…" : "Log in"} {!submitting && <ArrowRight className="ml-2 size-4" />}</Button><p className="pt-2 text-center text-sm text-muted-foreground">New to campus? <Link href="/register" className="font-extrabold text-primary hover:underline">Create an account</Link></p></form></CardContent></Card></div></div></AppShell>;
}
